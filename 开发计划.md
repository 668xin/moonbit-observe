# moonbit-observe 开发计划

## 项目重构路线

将原 `moonbit-stopwatch`（轻量 Stopwatch + Benchmark）重构为 `moonbit-observe`（运行时计时与指标观测 SDK）。

### 仓库名与包名

- **仓库**：`668xin/moonbit-observe`（GitHub）
- **包名**：`moonbit-observe`
- **对外引用**：`import "668xin/moonbit-observe"`

---

## 阶段 1：架构重构 & 时钟抽象层（7 天）

### 目标
- 解耦 Stopwatch 对 `@env.now()` 的硬编码依赖
- 建立 Clock 抽象层，支持系统时钟、单调时钟、Mock 时钟
- 重构 Stopwatch 核心类型，接入 Clock trait

### 任务清单

- [ ] 定义 `Clock` trait（`now_nanos() -> Int64`）
- [ ] 实现 `SystemClock`（基于 `@env.now()` 毫秒转纳秒）
- [ ] 实现 `MonotonicClock`（单调时间源）
- [ ] 实现 `MockClock` / `FakeClock`（手动推进时间）
- [ ] `ClockProvider` 全局/实例级时钟注入机制
- [ ] 时钟回退检测（`ClockBackwardDetector`）
- [ ] 重构 `Stopwatch`：接收 `Clock` 参数替代直接调用 `@env.now()`
- [ ] 默认兼容：`Stopwatch::new()` 默认使用 `SystemClock`
- [ ] 更新所有测试适配 Clock 抽象

### 产出物
- `src/lib/clock/` 模块（clock.mbt, mock_clock.mbt）
- `src/lib/core/timer.mbt`（重构后的 Stopwatch）
- `src/lib/core/ 模块结构`
- 全部已有测试通过

---

## 阶段 2：并发安全、标签计时、嵌套 Span（7 天）

### 目标
- 实现线程安全 Stopwatch
- 标签体系（Tag）
- 嵌套 Span 计时（父子层次）
- 阶段计时器（PhaseTimer）

### 任务清单

- [ ] `SharedStopwatch`：基于不可变 + 原子操作的并发安全包装
- [ ] `Tag` 结构体：key-value 标签体系
- [ ] `TaggedStopwatch`：携带标签的计时器
- [ ] `Span` 嵌套计时：父子层次，自动累计子树耗时
- [ ] `PhaseTimer`：连续命名阶段计时
- [ ] 单元测试覆盖并发 + 标签 + Span

### 产出物
- `src/lib/concurrent/shared_stopwatch.mbt`
- `src/lib/core/tag.mbt`
- `src/lib/core/span.mbt`
- `src/lib/core/phase_timer.mbt`

---

## 阶段 3：统计引擎、滑动窗口、基线回归检测（8 天）

### 目标
- 均值、方差、标准差
- 滑动窗口统计
- 简易对数直方图 + 分位数
- 基线持久化 + 回归检测

### 任务清单

- [ ] `StatisticalSummary` 聚合结构体
- [ ] `SlidingWindowStats` 时间/计数窗口
- [ ] `LogHistogram` 简易对数直方图（P50/P90/P99）
- [ ] `BaselineStore` JSON 持久化读写
- [ ] `RegressionDetector` 退化检测（比率/绝对值阈值）
- [ ] 单元测试覆盖统计 + 基线

### 产出物
- `src/lib/metrics/stat.mbt`
- `src/lib/metrics/sliding_window.mbt`
- `src/lib/metrics/histogram.mbt`
- `src/lib/baseline/store.mbt`
- `src/lib/baseline/detector.mbt`

---

## 阶段 4：报告序列化、持久化、完善示例（5 天）

### 目标
- ConsoleReporter 控制台输出
- JsonReporter JSON 序列化
- Snapshot 快照持久化
- CompositeReporter 多路输出
- 更新示例代码

### 任务清单

- [ ] `ConsoleReporter` 控制台格式化输出（可选单位/精度）
- [ ] `JsonReporter` 完整 JSON 序列化（含直方图、Span 树）
- [ ] `Snapshot` 快照结构体（序列化/反序列化）
- [ ] `CompositeReporter` 多 reporter 组合
- [ ] 更新原有示例（basic_usage, lap_demo, bench_demo）
- [ ] 新增示例：async_timer_demo、span_demo、regression_demo

### 产出物
- `src/lib/reporter/console.mbt`
- `src/lib/reporter/json.mbt`
- `src/lib/reporter/snapshot.mbt`
- `src/lib/reporter/composite.mbt`

---

## 阶段 5：全覆盖单元测试、文档、README 完善（3 天）

### 目标
- 测试覆盖全部 API 分支 + 边界场景
- README 完善
- 最终构建验证

### 任务清单

- [ ] 补齐所有模块边界测试用例
- [ ] 集成测试：多模块联动（如 Stopwatch + Histogram + Reporter）
- [ ] 更新 README.md
- [ ] 更新 README.en.md
- [ ] `moon build` + `moon test` 全部通过
- [ ] 推送至 GitHub/GitLink

### 产出物
- 全部模块单元测试
- 完善的中英文 README
- 无警告构建

---

## 里程碑汇总

| 阶段 | 内容 | 预计 LOC | 周期 |
|------|------|---------|------|
| 1 | 架构重构 & 时钟抽象层 | ~800 | 7 天 |
| 2 | 并发安全、标签、嵌套 Span | ~1,000 | 7 天 |
| 3 | 统计引擎、基线回归检测 | ~1,200 | 8 天 |
| 4 | 报告序列化、示例 | ~800 | 5 天 |
| 5 | 测试、文档 | ~1,000 | 3 天 |
| **合计** | **一期竞赛交付版本** | **~4,800 LOC** | **30 天** |

## 风险备忘

| 风险 | 应对 |
|------|------|
| 协程本地存储缺失 | 初期显式上下文传递，待生态成熟后追加隐式传播 |
| HDR 直方图复杂 | 一期简易对数直方图，二期完整 HDR |
| 时钟单调性 | MonotonicClock + 回退检测双保险 |
