# moonbit-observe —— MoonBit 运行时计时与指标观测 SDK

## 基本信息

- **项目名称**：moonbit-observe —— MoonBit 运行时计时与指标观测 SDK
- **参赛者**：668xin
- **联系方式**：15970327528
- **GitHub 仓库链接**：https://github.com/668xin/moonbit-observe.git
- **项目方向**：运行时计时与指标观测基础设施
- **是否为移植项目**：否（原创项目）

## 项目简介

moonbit-observe 是面向 MoonBit 语言生态原创开发的**运行时计时与指标观测 SDK**，提供 Stopwatch 计时、分布式链路计时、时序指标聚合、持续性能基准检测等生产级可观测性基础设施。

当前 MoonBit 生态缺少开箱即用、支持多阶段打点、并发安全的标准化计时与观测组件。开发者在业务程序、脚本、服务中进行耗时观测时，只能手动编写临时计时代码，缺少多阶段打点、异步任务计时、分位数统计、基线回归检测、人性化时间格式化等能力。

本项目核心定位为**运行时埋点计时观测工具**，服务应用开发过程中多阶段耗时统计、流程耗时观测、基准性能回归检测。与 moonbench（面向 `moon bench` 的源码级基准测试框架）使用场景、核心目标完全不同——moonbit-observe 面向程序运行时动态计时，适用于业务代码埋点、多流程分段耗时观测。

库完全基于 MoonBit 标准库构建，无重型外部依赖，可直接作为公共依赖引入任意 MoonBit 项目。

### clock — 时钟抽象层

- 定义 `Clock` trait：`now_nanos() -> Int64` 统一接口
- `SystemClock`：基于 `@env.now()` 的系统时钟实现
- `MonotonicClock`：基于单调时钟源，避免系统时间跳变导致的计时偏差
- `MockClock` / `FakeClock`：可注入虚拟时钟，支持手动推进时间，满足单元测试场景
- 时钟回退检测：检测系统时钟向后跳变并标记异常

### core/timer — 基础计时器

- 重构原有 `Stopwatch` 核心类型：解耦硬编码时间调用，接入 Clock 抽象层
- 支持启动、暂停、恢复、重置操作
- 多精度时间读取接口：纳秒、微秒、毫秒、秒级耗时原始数值
- 分段打点（Lap）能力：支持命名分段计时，保存每一段耗时与累计时间
- 智能时间格式化工具：自动适配最优时间单位（ns / μs / ms / s）
- 格式化工具 `format_time`：秒数 → HH:MM:SS.mmm
- `Stopwatch::elapsed_fmt()` 便捷方法

### concurrent — 并发安全封装

- `SharedStopwatch`：线程安全的 Stopwatch 包装器，适配 `moonbitlang/async` 多协程场景
- 共享计时器：多个协程可并发读取/控制同一个计时实例

### metrics/stat — 统计引擎

- 均值、方差、标准差计算
- 滑动窗口统计（时间窗口 / 计数窗口）
- 简易对数直方图：支持 P50 / P90 / P99 分位数近似计算
- `StatisticalSummary` 统一聚合结果结构体

### baseline — 性能回归模块

- `BaselineStore`：基线数据持久化（JSON 格式存储/加载）
- `RegressionDetector`：新旧测试对比，检测性能退化（比率阈值 / 绝对值阈值）
- 支持自定义告警回调

### span — 链路计时

- 嵌套阶段计时（Span）：父子层次结构，支撑简易调用链追踪
- `Tag` 标签体系：key-value 标签绑定，支持按标签聚合过滤
- `PhaseTimer`：命名的连续阶段计时（多阶段流水线）

### reporter — 报告组件

- `ConsoleReporter`：控制台格式化输出，支持自定义单位、精度
- `JsonReporter`：JSON 序列化导出（含分位数、窗口统计、Span 树等完整信息）
- `Snapshot` 快照结构体：可序列化/反序列化，支持快照持久化读/写
- `CompositeReporter`：多路输出组合

### prelude — 统一对外 API

- 一键导入所有公开类型和函数
- 与旧版本 `@lib.Stopwatch` 向后兼容过渡
- CSV / 轮转文件持久化（自动文件轮转、历史归档）
- 定时后台上报 `ScheduledReporter`（固定间隔自动导出）
- 完整 HDR Histogram 实现（高精度、可配置）
- Prometheus 格式输出适配器（直接对接 Prometheus 生态）
- 协程本地隐式上下文传播（待 moonbitlang/async 提供 ThreadLocal 能力后）

## 参考说明

本项目为原创项目，无直接移植目标。

参考主流生态同类库设计理念：

- Micrometer（Java 生态指标门面）
- Dropwizard Metrics（Java 生态指标库）
- OpenTelemetry（可观测性标准）
- Go runtime/metrics

本项目许可证：MIT
